# 單元 4 - 工作流說明

![封面](cover.png)

> 🕐 預估時長：20 分鐘

## 學習目標

完成本單元後，您將能夠：
- 區分「工作流 (Workflow)」與「聊天流 (Chatflow)」的適用場景
- 熟悉 Dify 工作流畫布 (Canvas) 的介面操作
- 掌握「開始 (Start)」節點的變數設定 (Input Variables)
- 學會使用「預覽 (Preview)」與「日誌 (Logs)」進行除錯

## 內容大綱

### 1. Workflow vs Chatflow

Dify 提供了兩種主要的編排方式：

| 特性 | 工作流 (Workflow) | 聊天流 (Chatflow) |
| :--- | :--- | :--- |
| **互動模式** | 單次執行 (One-off) | 持續對話 (Conversation) |
| **記憶能力** | 無 (執行完即結束) | 有 (具備上下文記憶) |
| **主要用途** | 數據處理、內容生成、自動化任務 | 客服機器人、個人助理 |
| **觸發方式** | API、排程、手動輸入 | 對話框輸入 |

**本單元重點在於「工作流 (Workflow)」。**

### 2. 工作流畫布 (Canvas) 介紹

- **節點 (Nodes)**：構成流程的基本單位（如 LLM, Code, Knowledge Retrieval 等）。
- **連線 (Edges)**：決定資料流向的線條。
- **縮放工具**：左下角可以放大、縮小或整理 (Auto Layout) 畫布。
- **測試運行 (Run)**：右上角的播放鍵，用於單次測試目前的流程。

### 3. 開始節點 (Start Node)

這是所有工作流的起點，我們在此定義「輸入變數」。

- **System Variables**：系統預設變數（如 User ID）。
- **Custom Variables**：自定義變數。
    - **Text**：短文字輸入。
    - **Paragraph**：長段落輸入。
    - **Select**：下拉選單。
    - **Number**：數字。

**範例**：如果您要做一個「文章摘要生成器」，您可以在 Start 節點定義一個 `article_content` (Paragraph) 變數，讓使用者在執行時貼上文章。

### 4. 除錯與發佈

- **測試運行 (Test Run)**：
    - 在開發過程中，隨時點擊測試。
    - Dify 會顯示每一個節點的 **輸入 (Input)** 與 **輸出 (Output)**，方便抓錯。
    - 如果某個節點亮紅燈，代表該節點設定有誤或執行失敗。
- **發佈 (Publish)**：
    - 當測試無誤後，點擊發佈。
    - 發佈後會生成 API，您可以將其整合到您的網站或 APP 中。

### 5. 版本控制 (Version History)
Dify 內建強大的版本控制功能。
- 每次發佈都會自動記錄一個版本。
- 您可以隨時回退到之前的任何一個版本，不用擔心改壞了救不回來。

---

## 📝 課後小測驗

> [!QUIZ]
> **Q: 如果我要製作一個「每週自動產生銷售週報」的應用，應該選擇哪種流程？**
> - [x] 工作流 (Workflow)
> - [ ] 聊天流 (Chatflow)

> [!QUIZ]
> **Q: 在工作流中，哪一個節點負責定義使用者需要輸入的資料（如文章內容、關鍵字）？**
> - [ ] 結束節點 (End)
> - [x] 開始節點 (Start)
> - [ ] LLM 節點
> - [ ] 條件分支節點

> [!QUIZ]
> **Q: 發佈 (Publish) 工作流的主要目的是什麼？**
> - [ ] 讓自己開心
> - [x] 生成 API 供外部調用或更新正式環境
> - [ ] 清除所有變數
